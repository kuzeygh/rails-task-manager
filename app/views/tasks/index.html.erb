<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>

<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.description %></p>
<% end %>

<% @tasks.each do |task| %>
  <%= link_to task.name, task_path(task) %>
<% end %>
